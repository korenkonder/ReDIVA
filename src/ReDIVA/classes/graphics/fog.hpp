/*
    by korenkonder
    GitHub/GitLab: korenkonder
*/

#pragma once

#include "../../../KKdLib/default.hpp"
#include "../../classes.hpp"

extern bool graphics_fog_init(class_data* data, render_context* rctx);
extern void graphics_fog_imgui(class_data* data);
extern bool graphics_fog_dispose(class_data* data);
