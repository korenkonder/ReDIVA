/*
    by korenkonder
    GitHub/GitLab: korenkonder
*/

#pragma once

#include "glitter.h"

extern bool FASTCALL glitter_texture_hashes_unpack_file(glitter_effect_group* a1, f2_header* header);
extern bool FASTCALL glitter_texture_resource_unpack_file(glitter_effect_group* a1, uint64_t data);
