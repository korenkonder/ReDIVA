/*
    by korenkonder
    GitHub/GitLab: korenkonder
*/

#pragma once

#include "glitter.h"

extern bool FASTCALL glitter_diva_effect_parse_file(glitter_file_reader* fr, f2_struct* st, float_t emission);
extern bool FASTCALL glitter_diva_effect_unparse_file(GLT, glitter_effect_group* a1, f2_struct* st);
