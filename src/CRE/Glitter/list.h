/*
    by korenkonder
    GitHub/GitLab: korenkonder
*/

#pragma once

#include "glitter.h"

extern bool glitter_list_pack_file(GlitterEffectGroup* a1, f2_struct* st);
extern bool glitter_list_unpack_file(GlitterEffectGroup* a1, f2_struct* st);
